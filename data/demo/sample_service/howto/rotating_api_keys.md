# Rotate an API key (Sample Service)

1. Navigate to **Settings → API Keys**
2. Click **Create new key**
3. Update your application to use the new key
4. Revoke the old key after verifying traffic is healthy
