# Rotate an API key (Sample Service)

1. Navigate to **Settings → API Keys**
1. Click **Create new key**
1. Update your application to use the new key
1. Revoke the old key after verifying traffic is healthy
