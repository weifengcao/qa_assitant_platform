# Universal Q&A Assistant Platform — Environment Variables
# Copy this file to .env and adjust values for your environment.
# None of these are required to run with defaults.

# ── Policy ────────────────────────────────────────────────────────────────────
# Path to the RBAC/deny-pattern policy YAML.
# Default inside Docker: /app/config/policy.yaml
POLICY_PATH=config/policy.yaml

# ── Data ──────────────────────────────────────────────────────────────────────
# Root directory for pack document files.
# Structure: DATA_DIR/<org_id>/<pack_id>/howto/...
DATA_DIR=data

# ── Audit ─────────────────────────────────────────────────────────────────────
# Where to write audit events: "memory" (default) or "file".
AUDIT_SINK=memory

# Path for the JSONL file when AUDIT_SINK=file.
# AUDIT_FILE_PATH=data/audit.jsonl

# ── Retrieval ─────────────────────────────────────────────────────────────────
# Doc index backend: "hybrid" (BM25 + vector, default) or "vector_only".
DOC_INDEX_BACKEND=hybrid

# Hybrid alpha weight: 1.0 = pure vector, 0.0 = pure lexical. Default: 0.75
DOC_INDEX_ALPHA=0.75

# ── Embeddings ────────────────────────────────────────────────────────────────
# Embedding backend: "hash" (zero-dependency, default) or "st" (sentence-transformers).
# "hash" is deterministic and requires no model download.
# "st" uses all-MiniLM-L6-v2 and requires: pip install sentence-transformers
EMBEDDING_BACKEND=hash

# ── App metadata ──────────────────────────────────────────────────────────────
# Shown in /health response.
APP_VERSION=0.1.0
BUILD_SHA=dev
